import { FileText, Plus } from 'lucide-react'
import { useState } from 'react'
import Filters from '../components/ui/Filters.jsx'
import PageHeader from '../components/ui/PageHeader.jsx'
import StatusBadge from '../components/ui/StatusBadge.jsx'
import { documents } from '../data/mockData.js'

export default function DocumentsPage() { const [search, setSearch] = useState(''); const data = documents.filter((item) => item.title.includes(search)); return <><PageHeader title="เอกสารข่าวสารกลาง" description="ประกาศ เอกสาร และข่าวสารที่เจ้าหน้าที่ทุกหน่วยงานเปิดดูร่วมกันได้" actions={<button className="btn-primary"><Plus size={14} />เพิ่มเอกสาร / ข่าวสาร</button>} /><section className="mb-4 grid grid-cols-2 gap-3 lg:grid-cols-5">{[['ทั้งหมด', 128], ['ประกาศ', 12], ['แนวทางปฏิบัติ', 48], ['แบบฟอร์ม', 32], ['ฉบับร่าง', 6]].map(([label, value]) => <div key={label} className="card py-3"><p className="text-[16px] text-slate-400">{label}</p><p className="mt-1 text-xl font-bold text-slate-800">{value}</p></div>)}</section><Filters search={search} onSearch={setSearch} compact /><section className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{data.map((item) => <article key={item.id} className="card transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start gap-3"><span className="rounded-xl bg-blue-50 p-3 text-blue-600"><FileText size={21} /></span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><p className="text-sm font-semibold leading-5 text-slate-800">{item.title}</p><StatusBadge>{item.status}</StatusBadge></div><p className="mt-3 text-[16px] text-slate-400">{item.category} · {item.owner}</p><p className="mt-1 text-[16px] text-slate-400">อัปเดต {item.date}</p></div></div></article>)}</section></> }
